name: newsletter
region: fra
services:
  - name: newsletter
    dockerfile_path: Dockerfile
    source_dir: .
    gitlab:
      branch: main
      deploy_on_push: true
      repo: aldofunes/newsletter-demo
    health_check:
      http_path: /health_check
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
    routes:
      - path: /
